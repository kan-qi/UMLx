nter the foldler unit and install the util model using 
mvn clean install.
Enter the folder SourceInstrumt and build the jar for SourceInstrument using 
mvn clean package.
Enter the folder LogcatModel and build the jar for LogcatModel using 
mvn clean package. 
After these steps above, you can find the jar files SourceInstrumt.jar, LogcatModel.jar in corresponding target folder.