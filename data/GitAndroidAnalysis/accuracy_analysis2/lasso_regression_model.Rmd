---
title: "Lasso regression report"
author: "XXX"
output:
  html_document:
    df_print: paged
  pdf_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

source("lasso_model.R")

modelData <- selectData("../android_analysis_datasets/android_dataset_6_29.csv")

plot(log(lambda_list),cvfit$cvm,xlab="log(Lambda)",ylab="MSE")
lines(log(lambda_list),cvfit$cvm,xlim=range(log(lambda_list)), ylim=range(cvfit$cvm), pch=16)
```

```{r}
#test and present your functions in regression_tree_model.R

```
