@case1
#Click a specific case
Send request to case database
Return the case information page
@case2
#Click "Reset password"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return password reset page
#Type in the new password and click "Submit"
Validate the information from password reset page
If validate failed, return invalid information message
If validate succeed, refresh the new password into the database and return reset succeed message
Return to case management page
@case3
#Click "Use Management"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed return user management page
#Click "Create user"
Return a user information page
# Type in the information and click "Submit"
Validate the information from user information page
If validate failed, return invalid information message
If validate succeed, insert the user into user database
@case4
#Choose a specific case
Send request to case database and receive the case information
Return a case information page
#Click "Edit"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return a case information page for editing
#Type in the new case information and click "Submit"
Validate the information from case information page
If validate failed, return invalid information message
If validate succeed, refresh the new information into database
Return a new case information page
@case5
#Click "User Management"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return user management page
#Check the status of user
If the status is activated, return wrong action message
If the status is inactivated, activate the user and change
#Click "Edit"
Return a user information page for editing
#Type in new information and click "Submit"
Validate the information from user information page
If validate failed, return invalid information message
If validate succeed, refresh new information into database
Return a new user information page
@case6
#Click "User management"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return user management page
#Selecta specific user and click "Activate"
Check the status of user
If the status is activated, return wrong action message
If the status is inactivated, activate the user and change the status of user in the database
@case7
#Click "User management"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return user management page
#Selecta specific user and click "Inactivate"
Check the status of user
If the status is inactivated, then return wrong action message
If the status is activated, then inactivate the user and change the status of user in database
@case8
#Click a specific document record
Validate the role of users
If validate failed, return unauthorized information
If validate succeed, send doc request to document database
Database return document and generate a preview of document
#Click "Download" or "Print"
Validate the role of users
If validate failed, return unauthorized information
If validate succeed, send request corresponding controller
@case9
#Click "Create user"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, generate a random password related to the username, which is the email address in the case
Insert the new user name and password in the user database and send request to case database
Connect case with a limited user and return generate succeed message
@case10
#Click "Add new"
Return activity description page
#Type in the information and click "Save"
Validate the description of activity
If description is blank, return "Description should not be blank " message
If validate succeed, insert activity into case
Update new activity record in case information page
@case11
#Click "Report"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return report generation page
#Choose the kind of report want to generate and type in necessary information
Validate the choice of report
If choice is blank, return "Report cannot be blank" message
If validate succeed, then validate the information from report page
If validate failed, return invalid information message
If validate succeed, send request to case database, document database and user database
Receive information and generate a preview of report
#Click "Print"
Send request to corresponding controller
@case12
#Click "Add new"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return document upload page
#Choose document and type in the description of the document and click "Submit"
Validate the information of description
If validate failed, return invalid description message
If validate succeed, insert document into document database
Update the case information page with new uploaded document record
@case13
#Click "Create new case"
Validate the role of user
If validate failed, return unauthorized action information
If validate succeed, return a case information page
#Type in the information and click "Submit"
Validate the information from case information page
If validate failed, return invalid information message
If validate succeed, insert case into case database return succeed message